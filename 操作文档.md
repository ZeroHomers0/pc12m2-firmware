# 操作文档：构建与烧写（手动命令）

> 面向 PC12M-2 操作者的命令式文档：**构建固件 + 两条烧写通道**（SWD / ISP）。
> 本文结构、工具选择和安全纪律与成熟的 PC6M-10 操作文档保持一致；板级结论、哈希、
> 路径和放行状态以 PC12M-2 自身证据为准。
> **构建与烧写命令唯一源**（2026-08-31）：`docs/w8/W8_SOFTWARE_OPERATION.md` 只保留工具索引。
> J-Link 一律调用仓库打包版 `tools\jlink\JLink.exe`。

## 概览

| 环节 | 方式 | 说明 |
|---|---|---|
| 安装依赖 | 手动安装（见 §1） | Git + Arm 工具链 + Python + Flash Magic |
| 构建固件 | `bash firmware/build.sh` | 产物 `firmware.bin / .hex / .elf / .map` |
| **SWD 通道** | `tools\jlink\JLink.exe` | 识别、只读备份；放行后用于烧写、校验和复位（见 §3） |
| **ISP 通道** | Flash Magic + USB-TTL | 已完成原固件读取；SWD 不可用时的备份和恢复通道（见 §4） |

> **执行顺序：先试 SWD，连不上再走 ISP。** 两条通道并存是因为应用固件可能复用调试脚；
> SWD 受运行态或保护状态影响时，ISP 仍可用于识别和只读备份。完成全部放行后，完整镜像写入
> 和校验优先使用 SWD。
>
> **当前状态（2026-09-02）**：原固件 `backup/pc12m2_orig.bin` 已核验（SHA-256 见 §3.3），
> 根目录副本已提交入库，第二物理副本已制作；P5 全量 A/B、113/113 覆盖、认证放行、产品信息
> 定制与 X/O 字形修复均完成。**自编固件已烧写入板并经用户实机测试通过**（2026-09-02，
> 未按 W8 分级流程执行，W8 分级实测已废弃）。重新烧写/发布新基线仍须完成 A/B 验证并核对 SHA-256，
> 新板首次烧写仍按 §3.4 放行门槛执行。

## 硬件前置（烧写前必读）

- 断开**市电 / 门极 / 功率负载**，板子仅接限流控制电；严格按 W8 阶段放行，不跨级。
- PC12M-2 的 P12 调试排针使用方式与参考板一致：

  | P12 脚 | 信号 | 用途 |
  |---|---|---|
  | 1 | VTref / 3.3V | SWD 参考电压；不得用调试器反向给整板供电 |
  | 2 | GND | 共地 |
  | 3 | **P2.10 / EINT0# / NMI** | ISP 引导脚：复位时拉低进入 Boot ROM |
  | 6 | TMS / SWDIO | SWD 数据；应用运行后可能被复用 |
  | 7 | **nRESET** | 复位 / connect-under-reset |
  | 8 | TCK / SWCLK | SWD 时钟；应用运行后可能被复用 |

- J-Link 操作后 MCU 可能仍处于 halt；只有复位并 `go` 后才会运行。
- 不能因为 PC6M-10 已实机验证而跳过 PC12M-2 的烧写放行门槛。

---

## 一、安装依赖

首次在新电脑上使用，按下列清单安装：

1. **Git for Windows**（提供 `bash.exe`）：<https://git-scm.com/download/win>
2. **Arm GNU Toolchain 14.2.Rel1（`arm-none-eabi`）**：
   <https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads>
   > 工程固定使用 14.2.Rel1；形成新的可复现基线前不要升级主版本。
3. **Python 3.12（64-bit）**及依赖：

   ```powershell
   py -3.12 -m pip install minimalmodbus pyserial unicorn
   ```

4. **J-Link 软件无需安装**。仓库已包含 `tools\jlink\JLink.exe`、运行库和 USB 驱动。
   如果设备管理器没有枚举探针，运行 `tools\jlink\USBDriver\InstDrivers.exe`。
5. **Flash Magic**：ISP 通道需要，使用命令行程序 `fm.exe` 或图形界面。

版本确认：

```powershell
arm-none-eabi-gcc --version
python --version
python -m pip show minimalmodbus pyserial unicorn
```

---

## 二、构建固件

在项目根目录使用 Git Bash：

```bash
cd firmware
bash build.sh
```

- 产物：`firmware/firmware.elf`、`firmware.hex`、`firmware.bin`、`firmware.map`。
- `build.sh` 自动查找 Arm GNU Toolchain 14.2.Rel1 和 PATH 中的工具链。
- 当前基线尺寸/SHA-256：`text 63964 / data 6312 / bss 2192`，`firmware.bin` SHA-256
  `449A13DB…63E2`（12p 自身基线；不要套用 PC6M-10 的尺寸或哈希）。

构建后在项目根目录执行：

```powershell
python test/run_tests.py
python tools/verification/verify_firmware_equivalence_12.py
```

通过标准：构建无错误、四种产物齐全、静态测试和十二相 A/B 验证全部 PASS，并记录、冻结
`firmware/firmware.bin` 的 SHA-256。原 BIN 已就位，CRC 和 A/B 测试失败属于明确阻塞，
不能忽略或用 PC6M-10 结果代替。

---

## 三、SWD 烧写：J-Link（日常主通道，首选）

> 所有命令都在 PC12M-2 项目根目录执行，并且只使用仓库内 J-Link。
> 当前本机尚未枚举到 J-Link 探针；本节保留与 PC6M-10 一致的标准流程，实际结果须重新记录。

### 3.1 连接探测（确认 SWD 链，不烧写）

`tools/jlink/check.jlink`：

```text
si SWD
speed 100
device LPC1765
connect
exit
```

执行：

```powershell
& ".\tools\jlink\JLink.exe" -CommanderScript ".\tools\jlink\check.jlink"
```

正常连接通常会出现：

```text
Found SW-DP with ID 0x2BA01477
Found Cortex-M3 r2p0, Little endian.
```

这是连接判据，不是 PC12M-2 当前已完成的实测记录。失败时先检查探针枚举、VTref、GND、
SWDIO、SWCLK 和控制电，再尝试 §3.2。

### 3.2 connect-under-reset（应用固件复用 SWD 脚时使用）

应用固件可能复用 P1.29/P1.30，使运行态 SWD 失联。将 nRESET 接至 P12-7 后使用：

```text
si SWD
speed 100
device LPC1765
SetRESET
sleep 200
connect
ClrRESET
sleep 200
exit
```

仅在普通连接失败且接线已核对后使用。不要为了恢复 SWD 而先擦除 Flash。

### 3.3 烧写前备份 + CRP 检查

优先用只读脚本连续读取两份并比较哈希：

```powershell
python tools/w8/w8_backup_orig.py
```

等价的 J-Link 关键命令为：

```text
w4 0x400FC040, 1
savebin backup\backup_orig_1.bin, 0x0, 0x40000
savebin backup\backup_orig_2.bin, 0x0, 0x40000
mem32 0x2FC, 1
```

PC12M-2 已归档记录应满足：

| 项目 | 期望值 |
|---|---|
| 长度 | 262144 B（256 KiB） |
| SHA-256 | `2BC608683992AD6DB2D3CB75129BF57EA37D6D681D7C2C0A132E064EBD271BD1` |
| CRP @ `0x2FC` | `0xFFFFFFFF` |
| 初始 SP | `0x100029A0` |
| Reset 向量 | `0x000001A9` |

> **铁律**：板上两次读取必须一致，并与找回的 `pc12m2_orig.bin` 一致；还要复制至第二处独立物理位置。
> 在此之前绝不执行 unlock、recover、erase 或 mass erase。CRP 若非 `0xFFFFFFFF`，立即停止。

### 3.4 完整烧写序列（erase → loadbin → verifybin → 复位运行）

> 本节序列已随用户实机验证执行过（2026-09-01/02，自编固件烧写入板并实机测试通过）。
> 新板首次烧写或重新烧录新基线时，仍须满足下方放行门槛后再执行。仓库保留
> `tools/jlink/flash.jlink`，流程与 PC6M-10 一致：

```text
si SWD
speed 100
device LPC1765
connect
savebin backup\pre_flash.bin, 0x0, 0x40000
mem32 0x000002FC, 1
erase
loadbin firmware\firmware.bin, 0x0
verifybin firmware\firmware.bin, 0x0
SetRESET
sleep 200
ClrRESET
sleep 200
go
sleep 500
exit
```

烧写放行门槛必须同时满足：

1. 找回 `backup/pc12m2_orig.bin`，长度和 SHA-256 与 §3.3 一致。（2026-08-31 已满足）
2. 原 BIN 已复制到第二处独立物理位置。（2026-08-31 已满足）
3. 上板读取的 CRP 为 `0xFFFFFFFF`，烧前备份哈希已核对。
4. PC12M-2 静态测试和 A/B 验证全部 PASS。
5. 待烧写 BIN 的尺寸、SHA-256 和验证报告已冻结。
6. `docs/w8/W8_TEST_MASTER.md` 明确放行对应阶段。
7. 市电、门极和功率负载断开，只接限流控制电。

全部满足并取得人工放行后才可执行：

```powershell
& ".\tools\jlink\JLink.exe" -CommanderScript ".\tools\jlink\flash.jlink"
```

关键输出：

| 输出 | 含义 |
|---|---|
| `Erase: ...s` | 擦除完成 |
| `Program&Verify ...s` | 写入及编程校验完成 |
| `Verify successful` | 板上镜像与文件一致 |
| `* Error` | 操作失败，立即停止并保存完整日志 |

### 3.5 烧写后核对与运行验证

以 `verifybin` 成功作为镜像一致性判据，再执行：

```powershell
& ".\tools\jlink\JLink.exe" -CommanderScript ".\tools\jlink\runcheck.jlink"
```

脚本会连接、读取寄存器及 MEMMAP、释放复位并运行。PC12M-2 的 PC、通用寄存器和初始化魔数
尚未形成实测判定表，必须以十二相反汇编和首次实测建立，不能复制 PC6M-10 的数值。

### 3.6 失败排查

| 现象 | 处置 |
|---|---|
| 未发现 J-Link 探针 | 检查 USB、设备管理器和驱动，必要时运行仓库内驱动安装器 |
| 找不到 SW-DP / 无法附着 CPU | 检查控制电、VTref、GND、SWDIO、SWCLK 和接触；保持 `speed 100` |
| 应用运行后 SWD 失联 | 使用 §3.2；仍失败则改走 ISP 只读探测 |
| CRP 非 `0xFFFFFFFF` | 立即停止；不得 unlock、recover 或 mass erase |
| `verifybin` 失败 | 停止流程，保存日志，检查供电、接线、目标文件及哈希 |
| 烧写后程序不运行 | 核对 halt、复位、向量表和 MEMMAP；不得直接带载排查 |

### 3.7 实机烧写记录（PC12M-2，2026-08-31）

- 原固件备份：ISP，Flash Magic 13.65 / `fm.exe`，COM7，9600 波特。
- 器件签名：`0x26013733`（LPC1765）；CRP：`0xFFFFFFFF`。
- 原固件长度和 SHA-256 见 §3.3 及 `backup/BACKUP_RECORD.md`。
- SWD：2026-08-31 记录时本机无探针枚举；后续用户自编固件已烧写入板（见下）。
- 自编固件：**2026-09-01/02 已烧写入板并经用户实机测试通过**（未按 W8 分级流程执行，
  W8 分级实测已废弃）；烧写细节未回填，实机通过以 `AGENTS.md` 为准。

---

## 四、ISP 烧写：Flash Magic（SWD 连不上时的解困通道）

> PC12M-2 原固件已通过 ISP 成功读取。当前只允许识别和读取，禁止擦除及编程。

### 4.1 接线（USB-TTL → 板）

| USB-TTL | 接 PC12M-2 | 作用 |
|---|---|---|
| TX | **P0.1 / RXD0** | 电脑发送至 MCU |
| RX | **P0.0 / TXD0** | MCU 发送至电脑 |
| DTR（如有） | **P12-7 / nRESET** | 自动复位 |
| RTS（如有） | **P12-3 / P2.10** | 自动进入 ISP |
| GND | **P12-2 / 板 GND** | 共地 |

- TX/RX 必须交叉；不要用 USB-TTL 的 VCC 给整板供电。
- 没有 DTR/RTS 时，将 P12-3（P2.10）接 GND，手动复位或重新上控制电。
- 进入 ISP 的关键是**复位瞬间 P2.10 为低**；识别完成后可解除短接。

### 4.2 关键坑：P0.0/P0.1 被 ADM2483（U5）RS485 占用

P0.0/P0.1 同时连接板上隔离通信电路，外部接收器可能与 USB-TTL 争用。ISP 无响应时：

1. 确认接在 MCU 侧并已共地。
2. 禁止板上收发器输出，或断开其隔离侧供电。
3. 核对 TX/RX 交叉、P2.10 拉低和复位时序。

不要通过擦除固件来排查串口接线。

### 4.3 Flash Magic 配置

| 参数 | 值 |
|---|---|
| Device | **LPC1765** |
| Crystal / MHz | **12.0000** |
| COM Port | 实际枚举端口；可运行 `python tools/w8/w8_serial_detect.py` |
| 波特率 | 从 **9600** 开始；PC12M-2 备份已验证该速率可靠 |
| 文件 | 编程时使用 Intel HEX；当前禁止编程 |
| DTR/RTS | 仅模块实际引出并正确接线时启用，否则手动进入 ISP |

放行后若需要把原始 `.bin` 转为 Intel HEX：

```powershell
arm-none-eabi-objcopy -I binary -O ihex firmware\firmware.bin firmware\firmware.hex
```

只读探测：

```powershell
python tools/w8/w8_isp_probe.py --port COM7 --baud 9600
```

该脚本只查询同步、Part ID、Boot 版本和 UID，不包含擦除、解锁或写命令。

### 4.4 执行顺序与纪律

1. **先识别**：确认 Part ID 为 `0x26013733`。
2. **先备份**：分块读取整片 256 KiB，重复读取关键块并比较。
3. **检查 CRP**：读取 `0x2FC`，必须为 `0xFFFFFFFF`。
4. **合并并核验**：运行 `python tools/w8/w8_combine_hex.py`，核对长度、向量和 SHA-256。
5. **制作第二物理副本**：确认副本可恢复后，才具备后续放行资格。（2026-08-31 已完成）
6. **最后才可能擦写**：必须同时满足 §3.4 全部门槛。

器件识别成功不等于备份完成，更不等于允许烧写。9600 波特读取整片耗时很长，必须保存日志，
不能因为进度慢而跳过重读校验。

### 4.5 识别成功的标志（Flash Magic）

- Part ID `0x26013733`（十进制 `637613875`）= LPC1765。
- 完整过程以 `backup/BACKUP_RECORD.md` 为准。
- 若识别失败：检查串口枚举、共地、TX/RX、P2.10、复位时序及板上收发器争用。

### 4.6 命令行方式（fm.exe）——PC12M-2 备份实测

`fm.exe` 安装位置因电脑而异，不在仓库中写死绝对路径。在其目录执行，并替换实际串口：

```powershell
.\fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --readsignature
.\fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --read="0x2FC,0x300,crp.hex"
```

常用只读 directive：

| directive | 说明 |
|---|---|
| `--readsignature` | 读取器件签名 |
| `--read="start,end,file"` | 读取区间到 Intel HEX |
| `--readserialnumber` | 读取唯一序列号 |
| `--quiet="file"` | 输出到日志 |
| `--help` | 查看当前版本的完整参数 |

以下命令具有破坏性，**当前禁止执行**：`--erasedevice`、`--erasesectors`、`--program`。

已知注意点：

1. LPC1765 不使用 FAIM，不要用 `--readfaim` 判断保护状态。
2. CRP 受保护时可能返回 `security violation in device`；这不是授权擦除的理由。
3. `fm.exe` 读取 Flash 末尾可能因结束哨兵越界报错；PC12M-2 已通过分块和重叠重读核对，
   具体边界见 `backup/BACKUP_RECORD.md`。
4. 9600 波特整片读写很慢；备份必须耐心完成，放行后的完整烧写优先使用可校验的 SWD。

---

## 常见问题

**Q：工具链安装在哪里？** `firmware/build.sh` 自动探测约定版本、常见目录和 PATH；
以 `arm-none-eabi-gcc --version` 输出为准。

**Q：J-Link 软件要安装吗？** 不需要。只使用仓库内 `tools\jlink\JLink.exe`；未枚举时运行
`tools\jlink\USBDriver\InstDrivers.exe` 安装驱动。

**Q：什么时候用 ISP，什么时候用 SWD？** SWD 可连接时优先用于高速读取、烧写和校验；
SWD 无探针或无法连接时使用 ISP。PC12M-2 当前已验证的是 ISP 只读备份，不是烧写。

**Q：烧写脚本会自动备份，是否可以直接运行？** 不可以。自动 `savebin` 不能替代已核验的原 BIN、
第二物理副本、完整 A/B 验证和人工放行。

**Q：为什么 PC6M-10 的烧写结果不能直接沿用？** 两块板 MCU 和外围结构接近，但 PC12M-2 的触发相数、
通道、部分寄存器映射和固件基线不同。流程可以复用，结果必须由十二相 BIN 和实测重新证明。

**Q：当前状态是什么？** P5 全量 A/B、113/113 覆盖、认证放行、产品信息定制与 X/O 字形修复
均完成；自编固件已烧写入板并经用户**实机测试通过**（2026-09-02）。W8 分级实测流程已废弃；
重新烧写/发布新基线按 §3.4 门槛执行并核对 SHA-256。
