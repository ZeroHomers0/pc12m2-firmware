# 操作文档：构建与烧写（手动命令）

> 面向操作者的命令式文档：**构建固件 + 两条烧写通道**（SWD ／ ISP）。
> 本文件以参考项目 PC6M-10（`D:\code\LPC1765FBD100\decompiled\操作文档.md`）为底本，
> 两板 MCU 与接线完全一致，**全部命令通用**；已按本项目（PC12M-2）适配路径与实测值。
> **构建与烧写命令唯一源**；文档导航见 `AGENTS.md`。
> 根目录 `.bat` 不通用，**已删除**；全部操作按本文件命令在「Git Bash 或 PowerShell」中执行。
> J-Link 一律调用仓库打包版 `tools\jlink\JLink.exe`（免安装，无需本机装 J-Link 软件）。

## 当前状态（PC12M-2，2026-08-30）

- **原固件备份已完成**：ISP 9600 分块读取整片 256 KiB，成品 `backup/pc12m2_orig.bin`，
  SHA-256 `2BC60868…BD271BD1`；方法/哈希/铁律见 `backup/BACKUP_RECORD.md`。
- **可编译基座已搬入**（自参考项目），`firmware\build.sh` 复现构建通过（见 §2）。
- 12 相固件逆向重建后，§3 SWD / §4 ISP 烧写命令直接可用（同一 MCU、同一接线）。

## 概览

| 环节 | 方式 | 说明 |
|---|---|---|
| 安装依赖 | 手动安装（见 §1） | 一次性；Git + 工具链 + J-Link 驱动 + Python |
| 构建固件 | `bash firmware/build.sh` | 产物 `firmware.bin / .hex / .elf / .map` |
| **SWD 烧写** | `tools\jlink\JLink.exe` | **日常主通道（首选）**：识别/备份/烧写/校验/复位（见 §3） |
| **ISP 烧写** | Flash Magic + USB-TTL | **解困通道**：SWD 连不上（固件复用调试脚/读保护）时用它识别/擦除（见 §4） |

> **执行顺序：先试 SWD，连不上再走 ISP。** 两条通道为什么都存在：应用固件会把
> `P1.29(SWCLK)→RS485 DE`、`P1.30(SWDIO)→AD0.4`，正常运行态 SWD 可能连不上。此时走
> **ISP（UART0 + P2.10）** 识别/擦除解困，之后再转回 SWD 高速写回。两条通道都保留，
> 一条受固件/保护状态影响时另一条可恢复。

## 硬件前置（烧写前必读）

- 断开**市电 / 门极 / 功率负载**，板子仅接**控制电**（W8 分级纪律，不跨级）。
- **P12 排针**线序（板上已确认，两板一致）：

  | P12 脚 | 信号 | 用途 |
  |---|---|---|
  | 1 | VTref / 3.3V | SWD 参考电压（ISP 时作参考即可，不必供电） |
  | 2 | GND | 共地 |
  | 3 | **P2.10 / EINT0# / NMI** | **ISP 引导脚**：复位时拉低进 Boot ROM |
  | 6 | TMS / SWDIO | SWD（烧写后可能被固件占用） |
  | 7 | **nRESET** | 复位：SWD connect-under-reset ／ ISP DTR 复位 |
  | 8 | TCK / SWCLK | SWD（烧写后可能被固件占用） |

- 全部操作完成后核心可能处于 halt（J-Link 烧写后未复位即 halt），需复位才运行。

---

## 一、安装依赖

首次在新电脑上使用，按下列清单手动安装（仅官方来源）：

1. **Git for Windows**（提供 `bash.exe`，构建用）—— <https://git-scm.com/download/win>
2. **Arm GNU Toolchain 14.2.Rel1（`arm-none-eabi`）**—— 编译工具链。
   官方安装器（约 250 MB）：
   <https://developer.arm.com/-/media/Files/downloads/gnu/14.2.rel1/binrel/arm-gnu-toolchain-14.2.rel1-mingw-w64-x86_64-arm-none-eabi.exe>
   （发布页：<https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads>）
   > 工程固定 14.2.Rel1，**实机验证前不要切到 15.x**。
3. **Python 3.12（64-bit）** + 依赖包：
   <https://www.python.org/ftp/python/3.12.10/python-3.12.10-amd64.exe>
   ```powershell
   py -3.12 -m pip install minimalmodbus pyserial unicorn
   ```
4. **J-Link 软件**：**无需安装**。仓库已打包免安装最小集 `tools\jlink`（`JLink.exe` +
   DLL + 驱动），全项目唯一调用路径。若个别机器插 J-Link 未识别（设备管理器无 COM 设备），
   运行一次 `tools\jlink\USBDriver\InstDrivers.exe` 装 USB 驱动。

版本确认（装好后重开终端）：

```powershell
arm-none-eabi-gcc --version
python --version
python -m pip show minimalmodbus pyserial
```

---

## 二、构建固件

需要 Git Bash（Git for Windows 自带）。在项目根目录打开终端（PowerShell 或 Git Bash）：

```bash
cd /d/code/PC12M-2/firmware && bash build.sh
```

或直接：

```bash
cd firmware && bash build.sh
```

- 产物：`firmware\firmware.elf / .hex / .bin / .map`。
- `build.sh` 自动定位工具链（约定版本 14.2 rel1 → 常见安装目录最新 → PATH）。
- 通过标准（2026-08-30 复现构建实测，搬入基座原样构建）：尺寸
  `text 62644 / data 3000 / bss 2188`；`firmware.bin` SHA-256
  `80e21528…cdbf`，与参考项目最新自编译固件（`evidence/reverse/reference/PC6M10_selfbuild.bin`）
  逐字节一致（改代码后哈希会变，属正常）。

构建成功后跑全量回归：

```bash
python test/run_tests.py
python tools/verification/verify_firmware_equivalence.py
```

---

## 三、SWD 烧写：J-Link（日常主通道，首选）

> J-Link 一律用仓库打包版 `tools\jlink\JLink.exe`。以下命令在项目根目录的
> PowerShell / Git Bash 执行；`.jlink` 脚本可存成文件用 `-CommanderScript` 跑。

### 3.1 连接探测（确认 SWD 链，不烧写）

`tools\jlink\check.jlink`（参考项目 2026-08-29 实测定型）：

```text
si SWD
speed 100
device LPC1765
connect
exit
```

执行：

```bash
"tools/jlink/JLink.exe" -CommanderScript tools/jlink/check.jlink
```

成功标志：

```text
Found SW-DP with ID 0x2BA01477
Found Cortex-M3 r2p0, Little endian.
```

**实测要点**：
- 用 **`speed 100`**（100 kHz）普通 `connect` 即可成功，**无需 SetRESET / nRESET**。
- 不要用 4000 kHz：实测 4000 kHz + `SetRESET` 反而报 `Failed to attach to CPU. Trying
  connect under reset.` → 连接失败（该现象是接线接触不良的典型表现，见 §3.6）。
- **失败先查四根主信号线（SWDIO/SWCLK/VTref/GND）接触是否良好**——重新插紧即恢复；
  不要一开始就怀疑 J-Link / 线序 / 供电 / 速率。

### 3.2 connect-under-reset（应用固件复用 SWD 脚时必用）

应用固件把 P1.29/P1.30 用作 RS485/ADC，正常运行态 SWD 失联。必须在 **nRESET（P12-7）**
控制下连接：`SetRESET`（拉复位）→ `connect`（进入调试态）→ `ClrRESET`（释放复位）。

```text
si SWD
speed 100
device LPC1765
SetRESET
sleep 200
connect
ClrRESET
sleep 200
exit
```

> **注意**：仅当**应用固件已在运行**（脚已被复用）时才需要 connect-under-reset。
> 固件被擦除后 / 空 Flash 态，普通 `connect`（§3.1）即可，不必拉复位。

### 3.3 烧写前备份 + CRP 检查

```text
w4 0x400FC040, 1              ; MEMMAP=1：映射用户 Flash 向量（默认停在 Boot ROM 时 0x0 是 Boot ROM）
savebin backup\pre_flash.bin, 0x0, 0x40000    ; LPC1765 Flash = 256 KiB = 0x40000
mem32 0x2FC, 1                ; CRP 字必须 = 0xFFFFFFFF（无保护）
```

> **铁律**：备份前**不要** `unlock` / `recover` / `mass erase`（LPC17xx 有 CRP1/2/3，
> 解除较高保护会整片擦除、原固件不可恢复）。

### 3.4 完整烧写序列（erase → loadbin → verifybin → 复位运行）

`tools\jlink\flash.jlink`（参考项目 2026-08-29 实测定型）：

```text
si SWD
speed 100
device LPC1765
connect
savebin backup\pre_flash.bin, 0x0, 0x40000   ; 烧前自动备份 256KiB 原 Flash（铁律：不备份不擦除）
mem32 0x000002FC, 1                           ; 板上 CRP 字，应为 0xFFFFFFFF（无保护）
erase
loadbin firmware\firmware.bin, 0x0
verifybin firmware\firmware.bin, 0x0         ; 全镜像读回校验
SetRESET
sleep 200
ClrRESET
sleep 200
go
sleep 500
exit
```

> **与旧版（4000kHz + 双重 connect）的差异**：4000 kHz 连接不稳定，100 kHz 普通
> `connect` 一次成功。`w4 0x400FC040,1`（MEMMAP）已移除——`loadbin` 走 flash loader
> 直写，无需先设 MEMMAP；`verifybin` 同样直读 Flash 不受映射影响。

执行：

```bash
"tools/jlink/JLink.exe" -CommanderScript tools/jlink/flash.jlink
```

关键输出：

| 输出 | 含义 |
|---|---|
| `Erase: …s` | 整片擦除完成 |
| `Program&Verify …s` | `loadbin` 写入 + 编程校验 |
| `Verify successful` | `verifybin` 读回与文件完全一致 → **烧写成功** |
| `* Error` / `O.K.` | 失败 / 通过 |

### 3.5 烧写后核对与运行验证

**核对**（在 `verifybin` 已成功的基础上，确认板上内容）：

```text
mem32 0x2FC, 1                ; = 0xFFFFFFFF 无保护
```

> `verifybin` 走 flash loader 直读 Flash，不受 MEMMAP 影响，以 `verifybin` 为准。

**运行验证**（确认固件确实在跑，`tools\jlink\runcheck.jlink`）：

```text
si SWD
speed 100
device LPC1765
SetRESET
sleep 200
connect
halt
regs
mem32 0x400FC040, 1
ClrRESET
sleep 200
go
sleep 300
exit
```

执行：

```bash
"tools/jlink/JLink.exe" -CommanderScript tools/jlink/runcheck.jlink
```

判定（参考项目 2026-08-29 实测值）：

| 信号 | 实测值 | 含义 |
|---|---|---|
| `PC = 000006EE` | 用户 Flash 代码区（非 Boot ROM `0x1FFFxxxx`、非空 Flash 死区） | 固件正常取指执行 |
| `R4/R5 = 3456ABCD` | 上电自检魔数（固件初始化段写入） | 初始化已完成 |
| `MEMMAP = 1` | `0x400FC040 = 00000001` | Flash 映射生效 |
| `go` → `Error: CPU is not halted` | 复位释放后 CPU 已自行运行 | 符合预期（固件本来就在跑） |

### 3.6 失败排查

| 现象 | 处置 |
|---|---|
| `connect` 找不到 SW-DP / `Failed to attach to CPU` | **首要查四根主信号线（SWDIO/SWCLK/VTref/GND）接触**——重新插紧即可（实测根因）。仍不行再查驱动 / J-Link 未插 / 板未供电 / 线序 |
| 4000 kHz 连接不稳、100 kHz 正常 | 属接线/线阻问题，**一律用 `speed 100`**（§3.1/§3.4 已定型） |
| 固件复用 SWD 连不上 | 必须 connect-under-reset（§3.2），否则走 ISP（§4）擦除后重连 |
| 擦除/写入报错 | 供电不稳、接线松、线长干扰 |
| `verifybin` 失败 | 先 `-check` 类探测排除连接问题，确认线序与供电 |

### 3.7 实机烧写记录（参考项目 2026-08-29；两板接线/时序相同，作为标准动作参照）

> 本项目原固件备份已走 ISP 完成（`backup/BACKUP_RECORD.md`），SWD 实机烧写记录待 12 相
> 固件重建后补充。以下为参考项目实录，核心经验对两板同样适用。

**故障 → 根因**：ISP 擦除后 J-Link 连接失败（VTref 3.29V 正常）。**根因 = 四根主信号线
接触不良**，重新插紧后恢复。

**排查过程的可用证据**：
- 4000 kHz + `SetRESET` → `Failed to attach to CPU. Trying connect under reset.`（接触不良典型表现）
- 100 kHz 普通 `connect` → `Found SW-DP with ID 0x2BA01477` → **成功**
- nRESET 非必需：空 Flash 态普通 connect 即可（connect-under-reset 仅固件运行时需要）

---

## 四、ISP 烧写：Flash Magic（SWD 连不上时的解困通道）

> 适用场景：**应用固件复用 SWD 脚，SWD 连不上时**；或想完全脱离 SWD 走串口恢复。
> ISP 可识别 LPC1765 并整片擦除；擦除后 SWD 即可稳定连接、高速写回。
> **用法**：SWD 解困后**转回 §3 SWD 高速写回**，不要在 ISP 里烧完整 256 KiB。

### 4.1 接线（USB-TTL → 板）

| USB-TTL | 接 | 作用 |
|---|---|---|
| TX | **P0.1** (RXD0) | 电脑 → MCU（ISP 收） |
| RX | **P0.0** (TXD0) | MCU → 电脑（ISP 发） |
| DTR | **P12-7** (nRESET) | Flash Magic 用 DTR 复位 |
| RTS | **P12-3** (P2.10) | Flash Magic 用 RTS 拉低进 bootloader |
| GND | **P12-2** / 板 GND | 共地 |

- **TX/RX 必须交叉**，且共地；VCC 只作电平参考，不用 USB-TTL 给整板供电。
- **USB-TTL 模块通常只有四根线：TX / RX / VCC / GND**。本流程**不用 VCC**，实际用 TX/RX/GND
  三根即可通讯；DTR/RTS 不是必需（见下）。
- **进 ISP 的关键 = 复位时 P2.10 为低**（P2.10 是 Boot ROM 引导脚）。**实测首选：把 P12-3
  （P2.10）与 P12-2（GND）用杜邦线直接短接**（实机验证，短接才进得去），复位后
  MCU 即进 Boot ROM ISP；识别成功后可松开短接。
- 复位动作任选其一：模块有 DTR 引脚就由 Flash Magic 用 DTR 复位；没有就手动短接 P12-7
  （nRESET）到 GND 一下复位，或断电再上电。
- 四线模块足够用：P2.10 拉低用手动短接代替 RTS；Flash Magic 里**不勾** `Use DTR/RTS`。
  只有模块同时引出 DTR/RTS 时，才可选 `Options → Use DTR/RTS to reset & enter ISP` 全自动。

### 4.2 关键坑：P0.0/P0.1 被 ADM2483（U5）RS485 占用

板的 P0.0（TXD3）/P0.1（RXD3）经 **U5 = ADM2483BRWZ 隔离 RS485 模块** 走 Modbus，
其接收器输出（RO）会驱动 P0.1，可能跟 USB-TTL 的 TX 相冲导致 ISP 握手失败。**任选其一**：

1. **断 U5 隔离侧供电（AVCC/5V）**——推荐，最干净；
2. 或把 RE# 拉高（禁止接收输出）；
3. 或在 **MCU 侧测试点 / U38 对应脚** 取线，确保 U5 不驱动该网。

### 4.3 Flash Magic 配置

| 参数 | 值 |
|---|---|
| Device | **LPC1765** |
| Crystal / MHz | **12.0000** |
| COM Port | USB-TTL 枚举出的口（`tools\w8\w8_serial_detect.py` 可查） |
| 波特率 | 从 **9600** 起（实测可靠但慢）；握手成功后可尝试提高 |
| 操作 | 勾选 **Erase + Program + Verify**（擦除可只选目标段；**务必先在 Read 页备份**） |
| 文件 | Intel **HEX**（`.bin` 需先转 hex） |
| Options | 勾选 **Use DTR / RTS to reset & enter ISP**（经典 LPC ISP 组合） |

**`.bin` → `.hex`**（工具链在 PATH 时）：

```powershell
arm-none-eabi-objcopy -I binary -O ihex LPC1765.bin LPC1765.hex
# 当前重编版同理：
arm-none-eabi-objcopy -I binary -O ihex firmware\firmware.bin firmware\firmware.hex
```

### 4.4 执行顺序与纪律

1. **先备份**（铁律）：Flash Magic **Read** 页把当前 Flash 读成文件存档，两份分两处保管；
   与备份金标准哈希不一致前，**绝不 Erase**。
   > 本项目原固件已备份（`backup/pc12m2_orig.bin`，SHA 见 `backup/BACKUP_RECORD.md`）；
   > 备份成品须复制到第二处独立物理位置。
2. **CRP 检查**：读 `0x2FC`（`Read` 该区间）应为 `0xFFFFFFFF`；若返回
   `security violation in device` 说明板上启用了读保护，必须先确认备份后再授权擦除。
   > 仓库备份文件 `0x2FC=0xFFFFFFFF` **不能**推断板内当前保护状态，以板上实读为准。
3. 只擦必要段；烧录用转好的 `.hex`，勾 **Verify** 核对。
4. 器件识别成功 ≠ 烧写成功：必须看到明确进度/完成提示并 Verify；指示灯长期不闪、进度
   不动时**不要无限等待**。
5. 整片 256 KiB 用 9600 会非常慢（本项目备份实测全片分块读取约 25 分钟）。**更合理的流程**：
   ISP 擦除/解困后转 **SWD（§3）** 高速写入和校验。

### 4.5 识别成功的标志（Flash Magic）

- Part ID 十进制 `637613875` = `0x26013733` = LPC1765；Boot ROM 版本 `2.4`。
- 若识别不了：核对 U5 处理、TX/RX 交叉、P12-7 是否接、共地、COM 口。

### 4.6 命令行方式（fm.exe）——实测

> 命令行版可整段复制执行，无需打开 GUI / 翻官方手册。**执行文件是 `fm.exe`**，
> 不是 `FlashMagic.exe`（后者是 GUI 版，带参数运行会挂起不返回）。

**位置**：`D:\software\Flash Magic\fm.exe`（Flash Magic 13.65 build 6730）。
**识别 USB-TTL 的 COM 口**：`python tools/w8/w8_serial_detect.py`（本项目实测 FTDI 0403:6001 = COM7）。

**Directive 速查**（`fm.exe -h` 全文）：

| 类别 | directive | 说明 |
|---|---|---|
| 配置 | `--device/-d "name"` | 器件型号，如 `LPC1765` |
| | `--crystalfreq "12"` | 晶振 MHz |
| | `--serialport/-p "COMx"` | 串口 |
| | `--baudrate/-b 9600` | 波特率（9600 实测可靠） |
| | `--hardwareispentry` | 用 DTR/RTS 进 ISP（四线模块无 DTR/RTS 则不用） |
| | `--quiet/-q "file"` | 输出到文件而非控制台 |
| 任务 | `--readsignature` | 读器件签名（识别） |
| | `--read/-o "start,end,file"` | 读内存区间到 hex 文件（`-o` 同义） |
| | `--erasedevice/-e` | **擦除整个器件**（内部先 Unlock + 全局擦除） |
| | `--erasesectors "n,m"` | 擦指定扇区 |
| | `--program/-i "file"` | 编程 hex |
| | `--verify/-v "file"` | 校验 |
| | `--blankcheck "s,e"` | 空检查（地址须覆盖整个扇区） |
| | `--timeouts "regular,long"` | 超时 ms |
| | `--readserialnumber` | 读器件唯一序列号 |
| 其他 | `-h/--help` | 全部 directive 列表 |
| | `--commandfile="f"` | 从文本文件读命令 |

**实测完整流程**（全部经 `--serialport COM7 --baudrate 9600`，`--device LPC1765 --crystalfreq 12`）：

```bash
cd "/d/software/Flash Magic"
# ① 识别（进 ISP 成功 = 下方 Signature 出现）
./fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --readsignature
#   → RESULT: Signature = 0x26013733 (637613875)   ← LPC1765
# ② CRP 检查（铁律，读 0x2FC 应为 0xFFFFFFFF）
./fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --read="0x2FC,0x300,crp.hex"
#   → 成功 = Hex file saved（0x2FC 空白 0xFFFFFFFF）
#   → ERROR: security violation in device = 板上启用 CRP 读保护（见陷阱②）
# ③ 擦除（仅在有备份/用户明确授权后）
./fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --erasedevice
#   → RESULT: Device erased
# ④ 验证擦除（重读 0x2FC，应恢复 0xFFFFFFFF 空白）
./fm.exe --device LPC1765 --crystalfreq 12 --serialport COM7 --baudrate 9600 --read="0x2FC,0x300,crp2.hex"
```

**本项目备份专用流程**（全片 256 KiB 分块读取，同一命令文件一次 ISP 会话）：

```bash
cd "/d/software/Flash Magic"
./fm.exe --commandfile="D:/code/PC12M-2/backup/read_full_9600.cmd"
# 命令文件必须纯 ASCII、无注释行（fm.exe 会因非法字符解析失败）
# 读 16 块到 backup/part00..15.hex 后，用工具合并校验：
cd /d/code/PC12M-2
python tools/w8/w8_combine_hex.py        # → backup/pc12m2_orig.bin + SHA-256
```

**实测陷阱**：

1. **LPC1765 不支持 FAIM**：`--readfaim` → `ERROR: device does not support FAIM`。
   别用 FAIM 查 SWD 禁用（FAIM 是 LPC8xx 等器件的，LPC1765 无此区）。
2. **CRP 保护下的行为**：启用 CRP 时 ISP **读被拒**（`security violation in device`），但
   **全局擦除允许**（`--erasedevice` 在 CRP1/2 下可解锁）。CRP 下无法备份板上固件 →
   按 4.4 纪律**必须先取得用户明确授权**再擦除（板上固件可能无任何本地备份）。
3. **9600 下整片 256 KiB 读写极慢**（本项目备份实测约 25 分钟）：擦除/读小范围可用 ISP；
   全片读写/校验一律转 **SWD（§3）**。
4. **擦除后 SWD 应可连**：固件与 CRP 清除后 SWD 脚释放。若 J-Link 仍 `Could not connect`
   （VTref 正常），**根因是四根主信号线（SWDIO/SWCLK/VTref/GND）接触不良**——重新插紧后
   **100 kHz 普通 `connect` 直接成功**（连 nRESET 都不需要）。先插紧接线，再谈别的排查。
5. **ISP 会话约 1 分钟收不到命令即超时跳回应用**：每次 fm.exe 运行前须重新进 ISP，
   分块读取要放**同一个命令文件**里连续执行。

---

## 常见问题

**Q: 工具链要装到哪？** `firmware/build.sh` 自动探测：约定版本 14.2 rel1 → 常见安装目录
最新版 → PATH。默认装到 `C:\Program Files (x86)\Arm GNU Toolchain arm-none-eabi\14.2 rel1`。

**Q: J-Link 软件要安装吗？** 不用。仓库打包了免安装最小集 `tools\jlink\JLink.exe`（全项目
唯一调用路径）。驱动缺失时运行 `tools\jlink\USBDriver\InstDrivers.exe`。

**Q: 烧写会自动备份原固件吗？** SWD 流程（§3.4）在 `erase` 前自动 `savebin` 到
`backup\pre_flash.bin`（256 KiB）；ISP 流程（§4.4）务必先用 Flash Magic Read 页手动备份。
本项目原固件备份已完成（`backup/pc12m2_orig.bin`）。

**Q: 什么时候用 ISP，什么时候用 SWD？** SWD 连得上就一律 SWD（快、可校验）。只有 SWD 连
不上（固件复用调试脚 / 读保护）时走 ISP 识别/擦除解困，再转回 SWD 写回。两条通道都保留，
是恢复原固件的双保险。

**Q: 需要安装 Flash Magic 吗？** 是。ISP 通道依赖它（免费，官方
<https://www.flashmagictool.com/>）。SWD 通道不需要。

---

## 附录：参考项目 PC6M 踩坑与 Bug 总结（两板共用，12 相分析/实测直接参考）

> 来源：参考项目 W8 实机记录（`docs/w8/`，已整体搬入本项目）。两板 MCU、接线、触发架构
> 一致，以下全部适用或直接是 12 相差异分析的输入。详细记录见各 `W8_*.md`；本文为速查索引。
> 分类：**A 固件级 bug**（反编译/复刻回归，12 相重编译同样要防）｜**B 原厂设计行为**（不是
> bug，别误修）｜**C 烧写/连接坑**（操作层面）｜**D 触发引擎**（12 相差异分析直接基础）。

### A. 固件级 Bug（反编译回归，已在 6 相固件修复——12 相重编译时逐一核对）

| # | Bug | 现象 | 根因 | 修复 | 原文档 |
|---|---|---|---|---|---|
| A1 | **ADC wait_done 指针算术** | LCD 闪屏 + 按键全死 | `g_adc` 是 `volatile uint32_t*`，`g_adc + 4` 编译成 **+16 字节** → 读 AD0DR0 而非 AD0GDR → 非 ch0 通道 DONE 永不置位 → 死循环 → WDT 复位循环（~1.6s 一次） | 先 `(uint)g_adc + 4` 转整数做字节偏移 | ISSUE_LOG 问题8 |
| A2 | **input_scan FIO 指针元素偏移** | 背光不亮 + 菜单死 | `DAT_00001974(uint32_t*) + 0x34` 按元素偏移编译成 `+0xd0` → 读到 FIO 无效地址 → key 恒 0 | `(uint32_t)DAT_00001974 + 0xNN` 字节偏移 | ISSUE_LOG 问题9 |
| A3 | **case1 三分支 FAULT 门控** | 首页上下键无效 | 三分支被错误嵌套进 `FAULT==0` else → 缺相时 FAULT 置位 → 上下键永不执行 | 三分支移出 FAULT 门控（原厂 `cbnz FAULT` 跳过的是 RUN/STOP 逻辑，三分支不受门控） | ISSUE_LOG 问题10 / DISP_SEL_FIX |
| A4 | **case3 枚举项环绕→钳位** | 控制方式三态互斥被破坏（定值按 UP 跳回通讯） | items 10-14 反编译成"超上限环绕到 0"，原 BIN 是**钳位在上限**；item0 CTRL_MODE 是例外（原厂故意环绕） | items 10-14 改钳位、item0 保持环绕 | ISSUE_FIX 08-30 问题5 |
| A5 | **Modbus 大端字节序错** | reg 0x1001 等读写值错误 | RTU 帧内 16 位字段反编译成小端拼接 `frame[4]\|(frame[5]<<8)`，原 BIN 是大端 `(frame[4]<<8)\|frame[5]`，共 4 处 | 4 处改大端拼接 | ISSUE_FIX 08-30 问题3 |
| A6 | **三级菜单编辑态不闪烁** | 编辑态值不闪烁（case3/case4） | 渲染分支缺原 BIN 两块：TIMEOUT3>0x1F4 超时擦除块 + 软起时间自动置 1 块；另整页重绘高亮 attr 曾误写死为 1 | 补回两块逻辑；高亮改 `(*MENU2==N)?1:0` 仅当前项 | ISSUE_FIX 08-28 / 08-30 问题1 |
| A7 | **恢复出厂只显示"M"** | 确认页只显示一个"M" | `ldr r0,[0x6AA0]` 是从字面量池**取 0x6AA0 处存的值 0x4D9C**，反编译误当"取 0x6AA0 处字节"→ 首字节 0x4D='M' | `disp_string((int)0x4d9c,…)` | ISSUE_FIX 08-30 问题2 |
| A8 | **复位"重启"坐标错位** | 复位/重启字样画到屏外 | 两处 `disp_string` 坐标反编译成 `(0,0,0)`，原 BIN 是 `(3,0xa,0)` | 坐标还原为 `(3,0xa,0)` | ISSUE_FIX 08-30 问题6b |
| A9 | **主屏开环 LED 错译** | 开环分支 LED 状态错 | `fio1_pin20_ctrl(1)` 被误译成 `(0)` | 还原为 `(1)` | ISSUE_FIX 08-28 |
| A10 | **CRP 布局冲突**（烧写前硬门槛） | 自编译固件 0x2FC 落在 `wd_feed()` 指令中（0x721A2255），直烧可能启用非预期读保护 | 链接时 0x2FC 被普通代码占用 | startup.s 加 `.crp` 段发 0xFFFFFFFF；ld 固定到 0x2FC；objcopy `--gap-fill 0xFF` | ISP_FLASH §8.4 / POST_FLASH |

> **A 类通用教训**（12 相重编译对照清单）：① 反编译源码中所有指针 `+N` 一律核对是字节还是
> 元素偏移（A1/A2）；② 分支嵌套结构不能想当然，用原 BIN 反汇编交叉核对门控范围（A3/A4）；
> ③ Modbus 帧字段大端序（A5）；④ `ldr r0,[pool]` 是取池中值，不是池地址字节（A7）；
> ⑤ 任何 `disp_string`/位控制参数与原文 BIN 逐参数比对（A8/A9）；⑥ 烧写前必须审计 BIN 的
> `0x2FC`（A10），这是四道硬门槛之一。

### B. 原厂设计行为（不是 bug——实机发现"异常"先对照此表，别误改代码）

| # | 行为 | 说明 | 原文档 |
|---|---|---|---|
| B1 | **DISP_SEL = 控制方式** | `0x10001655`：0 通讯 / 1 本地 / 2 定值。**只有"定值"分支处理 UP/DOWN 调 MANUAL**；本地/通讯分支不处理按键。首页上下键无效的根因2 | DISP_SEL_FIX |
| B2 | **PID 菜单三级无法切换** | MENU==7 的 MENU2/MENU3 导航门控是原程序固有逻辑（A/B 16 场景 BIN=ELF 一致） | ISSUE_FIX 08-30 问题4 |
| B3 | **本地模式无法设给定值** | 只有 DISP_SEL==2（定值）分支处理 UP/DOWN 增减 MANUAL；用户须在菜单切"定值"后才可调 | ISSUE_FIX 08-30 问题6a |
| B4 | **恒流时恒流 LED 不亮** | case3 item0 运行模式渲染：恒流时 P1.21=低是原程序固有行为（逐指令一致）；硬件链路（LED18B/限流）存疑待实测 | ISSUE_FIX 08-28 问题2 |
| B5 | **通讯参数面板修改需重启生效** | 反汇编确证通讯参数（地址/波特率/校验）需重启板子生效 | SOFTWARE_OPERATION §3.2 |

### C. 烧写/连接坑（操作层面，两板相同）

| # | 坑 | 现象 | 根因 / 处置 | 原文档 |
|---|---|---|---|---|
| C1 | **SWD 连不上** | J-Link 报 `Could not connect` | 固件 `adc_init()` 把 P1.30(SWDIO) 复用为 AD0.4、`uart3_init()` 把 P1.29(SWCLK) 用作 RS485 DE/RE，`main()` 复位后立刻执行——**设计冲突非硬件故障**。调速率/换接口/对调线全无效 | JLINK_DEBUG |
| C2 | **SWD 连不上时的初判错误** | 误认 SWDIO/SWCLK=P0.27/P0.28（PINSEL1） | 编译器生成 `load 基址+偏移`，单寄存器绝对地址（PINSEL3=0x4002C00C）不出现在二进制里，"搜索地址字节"数不出 → 误报 PINSEL 全 0。实为 P1.30/P1.29 归 PINSEL3 | JLINK_DEBUG §3.1 |
| C3 | **4000kHz 连不上、100kHz 成功** | 4000kHz+SetRESET 报 `Failed to attach to CPU` | 接线/线阻问题典型表现。**一律 `speed 100` 普通 connect**；失败先查四根主信号线（SWDIO/SWCLK/VTref/GND）接触 | ISSUE_LOG / 操作文档 §3.6 |
| C4 | **MEMMAP 向量重映射** | 烧写后 `mem32 0x0` 读 Boot ROM 向量（`10001FFC/1FFF0081`），疑似扇区 0 未擦净 | reset & halt 停在 Boot ROM，MEMMAP=0，地址 0 映射 Boot ROM。读真实 Flash 前先 `w4 0x400FC040,1`；`verifybin` 走 flash loader 直读不受影响 | POST_FLASH §4 |
| C5 | **9600 写 256 KiB 卡死** | 长时无进度、USB-TTL 灯不闪，等超 1h 终止 | **界面在运行 ≠ 烧写成功**。判定烧写未完成；ISP 擦除后转 SWD 高速写回 | ISP_FLASH §8.2 |
| C6 | **擦除后屏幕仍有字** | 擦除后 LCD 仍显示字符 | 显示器件保持画面，**不代表 Flash 未擦除**；以 Flash 读回 / `0x2FC` 读数为准 | ISP_FLASH §8.3 |
| C7 | **板内 CRP 读保护状态** | 读 `0x2F0..0x30F` 返回 `security violation` | 仓库备份文件 0x2FC=0xFFFFFFFF **不能**推断板内当前状态；CRP 下读被拒、全局擦除允许，须先备份 + 明确授权 | ISP_FLASH §8.1 |
| C8 | **P12 排针定义** | 初版文档误以为 P12-3 是复位 | 实机确认：**P12-7=nRESET、P12-3=P2.10(ISP)**；P12-7 必须接（只有 TX/RX/GND 进不了 ISP） | ISSUE_LOG 问题2 / ISP_FLASH §2 |
| C9 | **烧写后核心 halt + SWD 失联** | 烧写后不运行；运行后 SWD 失联 | 烧写后核心处于 halt **需复位才运行**；运行后 P1.30/P1.29 复用 → SWD 失联是设计行为，非故障 | POST_FLASH §6 |
| C10 | **继电器误吸合扫描误报** | Unicorn 误报"上电恒吸合" | preset 地址笔误（0x10000750 应为 0x1000172C，注释遗留旧地址）——测试脚本地址笔误会制造假 FAIL | STAGE1_CLOSE §2.1b |
| C11 | **Modbus 读不到 / CRC 错** | `No response` / `CRC error` | **RS485 A-B 反接是最常见坑**（先接一副读不到再对调）；地址/波特率/校验与面板一致；COM 口被占用 | SOFTWARE_OPERATION §3.2 |
| C12 | **没接过零 = 没触发脉冲** | 接同步源后仍无触发输出 | 接上过零输入 MCU 才进入"有电网"状态发触发脉冲——阶段 C 最常见失误 | SOFTWARE_OPERATION §6 |
| C13 | **示波器探头 1x/10x** | 读数×10/÷10 | 通道菜单里选对探头比例；测强电**必须差分/隔离探头**（普通探头会烧示波器） | SOFTWARE_OPERATION §4 |

### D. 触发引擎（12 相差异分析直接基础——逆向时对照）

| # | 结论 | 说明 | 原文档 |
|---|---|---|---|
| D1 | **TIMER1 ISR 注释"LCD 12864 动态扫描"是反编译误判** | 实为 **SCR 触发脉冲生成引擎**（240 步扫描=360°，6 窗口×40 步=60°）；LCD 实为 P1 口驱动、主循环刷新。**12 相差异分析的核心对象** | STAGE2_CODE_PREPLAN §0 |
| D2 | **G5=P0.19、G6=P0.16（非 P2.19/P2.16）** | 触发脚全集：G1-G6=P0.17/P0.15/P0.18/P2.9/P0.19/P0.16；12 脉波 P2.8/P2.7/P2.6/P2.5/P0.8/P0.7。旧文档 P2.19/P2.16 有误 | STAGE2_CODE_PREPLAN §2 |
| D3 | **无显式"12° 单脉"** | 固件输出 54μs 载波 @≈9.26kHz + 60° 窗口间隔；"12°=666.7/555.6μs 单脉"判据**作废**（变压器+门极整形后实测核对） | STAGE2_CODE_PREPLAN §2-3 |
| D4 | **触发架构** | EINT3(P2.13/TW) 过零锚点 → TIMER2.MR0 单次触发角延时 → TIMER1 240 步扫描；EINT1/2(P2.11/12) 只做相序自检（TU 先到→mode1 逆序、TV 先到→mode2 正序）；封锁/安全态=`gpio_outputs_set()` 12 脚全高 | STAGE2_CODE_PREPLAN §1 |
| D5 | **6 相窗口=主 2 门极 + 12 脉波 2 门极（同步跟随）** | 每窗口同时触发 2 主 + 2 十二脉波互补相；12 脉波是**跟随 6 窗口的从触发，非独立 12 相移相**——PC12M-2 是否扩展为 12 独立窗口是核心 diff 问题 | STAGE2_CODE_PREPLAN §3 |

> 详细反汇编/时序表见 `docs/w8/W8_STAGE2_CODE_PREPLAN_2026-08-27.md`（12 相差异分析的基础预案）。
