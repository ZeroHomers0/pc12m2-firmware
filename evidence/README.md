# evidence

原始证据存放处，只增不改。

- `hardware/`：12 相位板的 BOM、接线表、引脚图、手册等原始硬件证据（**待收集**）。
  参考项目 6 相位板对应资料在 `D:\code\LPC1765FBD100\decompiled\evidence\hardware\`。
- `reverse/`：原始反编译、反汇编和过程报告（待备份固件到位后生成）。

**注意**：原固件备份 `backup/pc12m2_orig.bin` 属敏感产物，存于 `../backup/`（已 gitignore），
不入库。
